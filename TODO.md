TODO

Goal:
One command syncs the notes you already track: newest version wins everywhere,
even when edited in both places. Sync only by Anki noteId. We track Anki notes
(one note -> N cards generated by Anki), never individual cards.

Three commands with distinct jobs (see "Agreed command model" below): the
import wizard brings Anki notes into the vault, the export wizard creates new
notes in Anki, and Sync is the only command that updates notes on either side.
Fields, tags and deck map both ways; notetype changes and card-level
properties are out of scope (ENT-07).

Card side is informational only: the `cards[]` array from `notesInfo` is never
addressed (there is no "update card #2" API). Per-card operations (e.g. suspend
one side of a reversed pair) are out of MVP scope; a card-level layer may be
added later alongside notes, not instead of them.

Note states (per note; state = existence x id x sync record x dirtiness):
- ankiOnly.neverImported: Anki-only, never imported, no sync record -> import (create file). Owned by the import wizard, not by Sync (Sync only touches tracked notes; untracked Anki notes are counted as "needs import").
- ankiOnly.fileDeleted: Anki-only, has sync record, file deleted in vault -> tombstone rule (SYNC-01/SYNC-02): resurrect iff Anki mod is newer than deletion.
- synced.clean: both sides, clean (mod and hash match records) -> do nothing.
- synced.ankiNewer: only Anki newer -> push Anki -> vault.
- synced.vaultNewer: only vault newer -> push vault -> Anki.
- synced.diverged: both dirty -> compare Anki mod vs file mtime, newer wins.
- linked.unenrolled: linked by ID but no sync record -> enroll (store mod + hash), then handle as synced.*.
- vaultOnly.unexported: vault-only, no ID, never uploaded -> export (create in Anki, write back ID + sync records).
- vaultOnly.unenrolled: vault-only, has ID, no sync record (fresh data.json, manual ID) -> match by hash, enroll, then handle as synced.*.
- vaultOnly.ankiDeleted: vault-only with ID + sync record, note gone in Anki -> delete file, purge records + tombstone (Anki deletion always wins).
- orphaned: nowhere, stale sync records -> purge records.
- Identity is per block, not per file: a deleted file and a deleted block inside a living file are both missing; never recreate either.
- Origin matters exactly once: an Obsidian-born note without ID must go through export (vaultOnly.unexported) first; afterwards origin is irrelevant.

Agreed sync mechanics:
- SYNC-01: tombstones on vault delete event (reverse path -> noteId index, persisted noteId -> deletedAt); cleaned on sync when the note is also gone in Anki.
- SYNC-02: no tombstone (deleted outside Obsidian): Anki newer than recorded mod -> resurrect, otherwise respect deletion (purge, no recreate).
- SYNC-03: Anki deletion wins unconditionally (AnkiConnect has no deletion timestamps); delete file even with vault edits.
- SYNC-04: tie-break Obsidian wins by default; settings syncTieWinner ("obsidian" | "anki") + syncTieThresholdSec = 60; ties are reported.
- SYNC-05: missing files/blocks are skipped, counted, never recreated.
- SYNC-06: diverged notes are resolved by newest-wins (vault push vs anki pull), counted separately as pushed / refreshed.
- SYNC-07: mtime (TFile.stat.mtime, ms) is the vault-side clock vs Anki mod (unix seconds); own writes are safe because verdicts go through hashes first.
- SYNC-08: force is explicit and per note, offered on every row of both wizards, off by default, and is the mirror of SYNC-08 on the other side: "Anki wins" in the import wizard (FORCE_PULL from synced.vaultNewer and synced.diverged, RESURRECT from ankiOnly.fileDeleted) and "Obsidian wins" in the export wizard (FORCE_PUSH from synced.ankiNewer and synced.diverged, EXPORT from vaultOnly.ankiDeleted). Where the command already takes the preferred side, marking the row changes nothing; the report names the forced count, which counts only the forces that overrode a guard.
- SYNC-09: deck is mapped in both directions. Vault path is the source of truth for the target deck (deckForPath); a path change issues changeDeck for the cards currently in the ledger deck only; an Anki-side deck change (read via cardsInfo, notesInfo has no deck) moves the file to deckFolder. A note with cards in more than one deck is reported as ambiguous, never collapsed.
- SYNC-10: ignored directories are plugin-wide: no scan, no index, no ID filling, no purge, no import target. A ledger record whose file sits in an ignored folder is reported as out of scope and never purged, so the setting can never become a data-loss switch.
- UX-01: ux/persona.md is the reference for UX decisions (Kasia primary, Tomek secondary); anti-goals listed there are out of scope.

Agreed model regime:
- MODEL-01: export targets the source model (extra.model); new Obsidian notes always go to built-in Basic (no reverse, no cloze detection, no model picker in MVP).
- MODEL-02: models are assured on demand, only for the notes an export actually creates: the export plans first, collects the models its creates need, checks exactly those (modelNames + modelFieldNames per UC-20a), creates only the missing built-ins, and drops the notes of a model that is mismatched or name-colliding, counted in the report ("skipped on model mismatch"). A push reuses the Anki note's own model, which is present by definition, so UC-25a does not assure pushes; the real-schema check for pushes is UC-33a and the mapping remedy is UC-36. The all-built-ins check (ensureDefaultModels) belongs to the first-run modal (UC-39) and is never run as a side effect. User templates are never touched.
- MODEL-03: cloze means the built-in "Cloze" model only; cloze notes carry Anki's native fields (text, back_extra), so export is identity mapping with no field translation (live schema TBD, verify against Anki during UC-13: Text + Back Extra).
- MODEL-04: mapping targets mirror Anki built-ins only: Front, Back, Text, Back Extra (+ Skip as UI chrome, not a field).
- MODEL-05: exact-name (case-sensitive) auto-mapping; user maps surplus fields only; unmappable stays Skip; auto rows read-only, no override (auto rows live in advanced view, hidden by default — see UC-38 mapping visibility).
- MODEL-06: Add Reverse always Skip (card-generation control flag, never content); visible greyed out in advanced view only, hidden by default.
- MODEL-07: drop spaced (done: entity, parser, import branches, spaced regex, tests, README), drop Source entirely (done: setting, constant, -source variants, link injection); code-highlight still open (setting, medias, base64 constants ~437KB, containsCode plumbing).
- MODEL-08: vocabulary is Note everywhere in our code (entities, services, wizard, form). "Card" remains only for Anki-generated card counts in user-facing reports and for AnkiConnect protocol action names (notesInfo, addNote, ...), which are not ours to rename.
- MODEL-09: note-form block (fence ```note-form): one envelope, per-model field sets (Basic: front/back; Cloze: text/back_extra; custom: mapped fields). `model` is a required key; the writer always writes it (default Basic for new notes, never guessed cloze). The reader treats a missing `model` as Basic. Only editable fields are rendered by the form; `id` is hidden after assignment but round-trips in YAML. Unknown keys are preserved, never deleted. The content hash covers all synced fields + model, not just front/back/tags.
- MODEL-10: no migrations in MVP, period. There are no production users: dev notes, dev blocks and dev data.json may be discarded. No migration code is written for data.json, blocks, fences or mappings. A migration strategy (if ever needed for real users) is explicitly out of scope until post-MVP.
- MODEL-11: tags are per note (YAML tags field); there is no global flashcards tag. The settings.flashcardsTag key dies with the legacy cut (see UC-16).
- MODEL-12: supported built-ins are Basic, Basic (and reversed card), Basic (optional reversed card), Basic (type in the answer) and Cloze. Image Occlusion is explicitly out of MVP (known-but-unsupported: skipped with a direct report, never silent); masks need a canvas UI, which is a separate project.

Agreed entity shape (Anki mirror):
- ENT-01: abstract `AnkiNote` base (noteId?, modelName, fields, tags, deckName, lifecycle status) + subclasses per behavior: `BasicNote` (reverse as flag, not a class), `ClozeNote` (cN numbering), `CustomMappedNote` (generic class constructed from a pack, see ENT-04).
- ENT-02: lifecycle = one xstate v5 machine, pure transitions only (no live actors; hundreds of notes x interpreters is waste without gain). Machine is the single source of truth for allowed operations per state; called as transition(status, event) from import/export/sync. Mermaid diagram generated from the machine definition.
- ENT-03: persisted record per noteId: { status, updatedAt, lastMod, lastHash, v } (domain record, not an xstate snapshot: snapshots embed version-specific internals and break rehydration after machine/library upgrades). `status` stores the descriptive state name (e.g. "synced.clean"). `v` is the migration hook.
- ENT-04: pack JSON = one file per model (packVersion: 1, modelName + field mapping + derived formTemplate; never deck info). Built-in packs ship with the plugin (Basic, Cloze); user packs are created by the wizard for unknown models and reused silently on next import. A model without a pack is skipped by Sync + reported, never guessed.
- ENT-05: entity declares `formTemplate` (layout + editable/hidden fields); React components live in the gui layer (BasicNoteForm, ClozeNoteForm), never in entities. Render = f(type, lifecycle status): components may show per-state banners (e.g. synced.diverged conflict, vaultOnly.ankiDeleted, ankiOnly.fileDeleted resurrected).
- ENT-06: both wizards share code by construction: a wizard shell (page indicator, footer, cancel/back/next, progress) and a notes table (pagination, search, selection, per-row force toggle) live in a shared gui folder and are used by the import wizard and the export wizard; the two differ only in scope source (Anki deck vs vault path) and direction.
- ENT-07: property mapping matrix - fields, tags and deck are mapped both ways; notetype changes are unsupported (no AnkiConnect API, report only); card-level properties (suspend, flag, scheduling) are out of MVP.

Agreed command model (the reason this plugin exists; one line per command):
- `Import deck from Anki` (wizard): Anki to vault for a chosen deck. Pull only: it never writes to Anki, never purges, never resolves deletions, and never overwrites a note whose vault side is newer unless the user forces that single note. It discovers models, maps fields, creates missing packs, shows the direction of every change, and writes the result report.
- `Export deck from Obsidian` (wizard): the exact mirror of the import wizard, vault to Anki for a chosen deck. Scope: one Anki deck, showing how many vault notes map to it and how many already exist in Anki. It creates missing notes, pushes what changed in Obsidian, and never guesses: a note edited in both places, a note newer in Anki, and a note deleted in Anki wait for the user, named in the report as "use Sync" or "use Obsidian wins" - the per-row force ("Obsidian wins", the mirror of "Anki wins", off by default, plus a bulk button) takes the Obsidian version regardless of state, including recreating a note deleted in Anki.
- `Sync`: the whole-ledger sweep over everything already tracked - bidirectional, newest version wins, no action when the ledger is empty. It is the command that resolves edits made in Anki in bulk, plus deletions, deck moves and tombstones, without asking per note. Also available for a single note (active file).
- Rare actions stay in the command palette (including `Purge ledger`); the single ribbon icon is Sync.
- Each command resolves its own states through its own resolver over the one transition table (`importActFor`, `exportActFor`, `syncActFor`), so the three commands are mirrors of each other rather than three policies: a wizard never resolves a conflict by clocks, only the user (force) or Sync does.

Work items (order: cuts, then mechanical renames, then behavior on clean code):

How to work this list (binding for every UC below):
- UC numbers follow execution order; work top to bottom.
- Definition of done per UC: `npx tsc --noEmit`, `pnpm run lint`, `pnpm test`, `pnpm run build` all green; nothing else touched.
- Each UC states its done-when up front (red test first per AGENTS.md); "re-validate" means existing tests green with no logic change, nothing more.
- Phase 0b renames change names only: no assertion changes, no logic changes. If a rename forces a logic change, the UC is misscoped — split it.

Phase 0a - cuts (no user-visible change):
Removed the code nothing used: the legacy markdown builders, the flashcardsTag chain, the code-highlight feature with its ~437KB of base64 constants, and unused test helpers. Done first so every later change happens on code that is actually reachable.

Phase 0b - renames (mechanical, no behavior change):
Renamed the vocabulary to the Anki mirror: the flashcard-form fence became note-form and Card became Note across services, the wizard and the tests, with a green gate after each. Old fences are dropped rather than migrated (MODEL-10: no production users, no migration code).

Phase 0c - Anki-mirror foundation (behavior, on clean renamed code):
Built the mirror itself: the xstate note-lifecycle machine as the single source of truth for what a note may do, the persisted ledger record, the AnkiNote hierarchy, per-model note-form shapes with saved packs, and the unknown-model flow in the wizard. This is what makes the three commands mirrors of one policy instead of three separate ones.

Phase 1 - ledger, matching, export flows:
Made both directions real: a vault-side export that creates notes and writes the Anki id back, and an import that only pulls, with every note's direction and consequence named before the click, a fence-aware vault index, Purge ledger, and a round-trip test that caught a real duplicate file. Along the way it fixed an export that aborted outright on modern AnkiConnect, and stopped exporting one note from creating three unrelated note types.

Phase 1b - code quality toolchain (local only, no network, strict):
Made quality non-negotiable: strict TypeScript, type-aware ESLint with sonarjs and complexity ratchets, Prettier, knip, jscpd on src, dependency-cruiser with no-circular, one pnpm run check gate, and a dev-only data reset behind a separate build entry. It also fixed real damage found on the way - nine super-linear regexes that could freeze the editor, "[object Object]" error messages, untyped AnkiConnect responses - and deleted dead code, so the two-sided flows rest on a codebase that can be trusted and safely changed.
- [ ] Ratchet targets for the complexity metrics, named offenders to tighten as each is refactored: syncDeckNotes (17 cyclomatic, 19 cognitive, 116 lines, 7 parameters), classifyNoteLifecycle (14 / 19), ImportWizard (221 lines), NotesPreview (242), FieldMapping (126), DeckSelection (119) - UC-28 rewrites syncDeckNotes, UC-35/36a the wizard components, UC-36 the export page, so the ceilings drop as a side effect of that work

Phase 1c - decision table, guards and honest reports (local only, no behavior regression):

The lifecycle machine answers "is this transition legal"; it never answered
"what does this command do with this note in this run". That second question
lived in three resolvers, ~17 runtime guards spread over three services, and
four places where a note was neither written nor counted. This phase makes the
answer a table, proves the table is complete, and closes the silent outcomes.
The order matters: the document and the proof come first, so every later
production change is already visible in the table.

- [ ] UC-25h: the decision table as data, replacing the three resolver switches.
  src/entities/sync-decision.ts owns one row per (command, status, isForced):
  { act: NoteLifecycleEvent | "OUT_OF_SCOPE", owner, rationale }. OUT_OF_SCOPE is
  explicit and named ("this is Sync's business, not the wizard's"), so an empty
  cell stops being possible and "this command does not touch that" stops
  looking like a forgotten default case. syncDecisionTableMarkdown() prints it
  to docs/sync-decision-table.md and the anti-drift test compares the file with
  the generated text, the same pattern as the mermaid test at
  tests/services/note-lifecycle.test.ts:213. The three resolvers are repointed
  at the table in one mechanical step and then deleted at the end of this UC, so
  the table is the only entry point and nothing can grow a second policy behind
  a facade; the call sites (import.ts, export.ts, sync.ts) and the resolver
  tests end up calling syncDecisionFor directly. The row's rationale is the
  user-facing text: the same sentence is the justification in the generated doc
  and the badge in the preview, so a label and a policy cannot drift apart (the
  badge wiring itself is UC-25k). Done-when: doc in the repo, anti-drift green,
  zero behavior change (442/442, no assertion touched), no facade left
- [ ] UC-25i: four property tests that make the table trustworthy. (1) Totality:
  every (command x status x isForced) has a row; a missing row fails, never an
  accidental undefined. (2) Confinement at two levels: the resolver sets
  (import never PUSHes, export never PULLs, sync invents nothing) AND the
  AnkiConnect request log from the existing mock (a full executeImport issues no
  addNotes and no updateNoteFields; the export id write-back is a declared
  side effect). (3) Reachability with @xstate/graph: every (state, event) pair
  is either emitted by some table row or explicitly marked unreachable - that
  finds a state nothing leads to and an event nobody sends. (4) The three
  ad-hoc resolver describes become one test.each over the table, rows named
  "given <status> when <command> then <outcome>". Done-when: four tests, no
  any, no it.skip, every status and event accounted for
- [ ] UC-25j: three real bugs outside the policy table, counters and tests
  first. Missing media is dropped by `if (!data) continue` (media.ts:62-65), so
  the file is written with a dangling reference and the report says nothing; a
  fence parse failure in export is silent (export.ts:72-86), so a note vanishes
  from the export with no trace; and purgedRecords is computed and never shown
  (sync.ts:443-467), which is exactly the "report explains every number"
  criterion. None of the three needs the table - each needs a named counter and
  a literal-string test, and when the guard table lands in UC-25m the counters
  become those guards' reasons rather than throwaway work. Done-when: three
  counters exist, each has a test on its literal text, and no terminal outcome
  is silent
- [ ] UC-25k: the badge is the rationale. The preview renders row.rationale from
  the table instead of its own sentences, so all 11 literals leave
  NotesPreview.tsx (six states, three force variants, two accessible labels - the
  accessible labels are in scope because they are the same message for a screen
  reader, today hand-duplicated). The 10 GUI assertions keep their text and
  only change their source. Two new tests: the component contains no
  user-facing sentence, and every row the preview can reach has a non-empty
  rationale. Two things stay in the component on purpose: the aggregate
  "nothing selected yet: 2 notes with newer Obsidian edits" reasons, because
  they describe a whole selection rather than one row, and the file path in
  "up to date - imported (<path>)", which needs a runtime value and stays a
  composed exception until someone decides on a placeholder. Done-when: the
  11 literals are gone, the 10 assertions pass unchanged, both new tests green
- [ ] Next: UC-28. The first four items above are deliberately foundation and
  bug fixing, and none of them changes behavior. The persona-visible win is
  UC-28, rewritten on top of the finished table instead of on top of three
  switches. The four items below are marked (after UC-28, because ...) and
  must not be started earlier
- [ ] UC-25l: honest reports on the Sync side (after UC-28, because UC-28
  rewrites exactly this code and the counters are rewritten with it). The two
  holes that survive from the original inventory: counts.refreshed is set to
  eligible.length BEFORE the delegated import filters, so the report can claim
  a refresh that never happened (sync.ts:216-221), and an untracked Anki note
  with no linked block vanishes from Sync with no counter (sync.ts:121-126).
  Done-when: refreshed is the number the import actually wrote, the untracked
  note is counted with a named reason, and UC-28's aggregate report carries both
- [ ] UC-25m: guards as data (after UC-28, because the scope guard is the
  snapshots question, which is UC-28's job). Guard = { id, appliesTo, blocks,
  reason, mvp: "supported" | "reported" | "unsupported" } and the three command
  paths ask the table instead of carrying if statements - today 17 guards, some
  checked twice (the pack check runs in sync.ts:130-133 and again in
  import.ts:450-474). The rule MODEL-12 promises in prose becomes code: mvp
  "unsupported" means a mandatory counter, never a silent skip, visible both in
  the table and in the report with a named reason. Done-when: all 17 guards
  have a row, the duplicate pack check is gone, every row has a test
- [ ] UC-25n: every action that changes a note's state, in the same document as
  a second section (after UC-28 and UC-30/31, because DELETE_FILE and the
  tombstone rows arrive with them). Inventory: import write, export create,
  export push, the write-back of a new Anki id into the block, ENROLL (three
  distinct paths), purge, DELETE_FILE, RESURRECT, fill-id-by-hash, baseline
  write-back. For each: the states it can fire in, its producer, and the UC
  that owns it. Three of them have no table cell today - DELETE_FILE has no
  producer at all (Sync does not handle vaultOnly.ankiDeleted), purge and
  fill-id-by-hash bypass the resolver entirely - and they become named Phase 2
  entries instead of invisible code. Done-when: the section is complete against
  the code, enforced by the same anti-drift test, and every gap has a Phase 2
  reference
- [ ] UC-25o: one classification snapshot for preview and execution (after
  UC-25k, because the label the user read is then literally the policy that
  runs, which is what makes this worth proving). The preview
  (NotesPreview.tsx:273-293) and the execution (import.ts:477-501) classify the
  same note separately, so a stale index can make a badge promise "overwrites
  your file" while the run does something else. The index and the block hashes
  are read once and carried into the execution request, and a test with a
  deliberately stale index asserts badge and run agree. Done-when: that test is
  green and cannot regress silently

Not in this phase: the scan/recreate command (deferred, as decided), any change
to Sync's scope (that is UC-28, which runs in the middle of this phase, so the
four items marked "after UC-28" close the phase rather than precede it),
syncTieThresholdSec, a placeholder for runtime values in a rationale (the
up-to-date badge keeps composing its file path in the component for now), and
the legacy formats - the old flashcard-form fence, the Obsidian-* model names
and the spaced/source markers are not detected and not reported; MODEL-10 says
no migrations, and the plugin is not going to guess at content it no longer
reads.

Bugs found after the current phase (not tied to a phase):
- [ ] import wizard: it does not create subdecks, it should create nested subdecks as deep as Anki can hold them. What to look at: the import path never calls createDeck (only the export path does, from ensureDecks), the selected deck's `::` chain is only flattened into the vault folder path by deckFolder, and the per-deck search sends `deck:"Name"`, which in Anki does not match `Name::*` subdecks - so a parent deck that only holds subdecks can read as empty

Chores found after the current phase (not tied to a phase):
- [ ] rename the plugin to ObsidiANki everywhere the old name survives. Inventory: manifest.json (display name "Better Flashcards", id `better-flashcards`), package.json name `flashcards`, `pluginFolderName = "better-flashcards"` in src/services/note-packs.ts, rollup.config.js (four paths into the test vault), .gitignore (the test-vault plugin path), docs/BUILD_AND_RELEASE.md (5), docs/CONTRIBUTING.md (4), README.md (2), AGENTS.md (1), docs/test-vault/.obsidian/community-plugins.json (the tracked fixture that enables the plugin). Traps: (a) changing the manifest id changes the install folder, so Obsidian installs a *second* plugin and the old data.json with the whole ledger stays behind - the safe path is to change only the display name and move the id only together with a migration that reads the old folder; (b) note-packs writes saved packs under the plugin folder, so an id change orphans every pack a user saved unless that path is migrated; (c) main.js and dist/ are build artifacts, nothing to rename there. Deliberately not renamed: the CSS prefix `flashcards-` (122 occurrences in styles.css plus the class maps) and the icon id `flashcards` - internal identifiers, not the product name, and renaming them is pure churn

Phase 2 - sync core (ledger-driven, deck mapping, deletions):
- [x] UC-26: purge scope fix (data loss) - Sync asked only notesInfo for records not seen in the snapshot decks and removed their blocks + records, so notes exported to a deck without a snapshot were deleted from the vault. Purge now confirms absence with a batched notesInfo per candidate; foreign records stay untouched
- [x] UC-27: synced.diverged newest-wins (mod vs mtime) (isAnkiNewer in note-push: anki mod * 1000 vs TFile.stat.mtime; Anki newer -> export reports it, Sync pulls; vault newer -> both push)
- [ ] UC-28: ledger-driven Sync (scope = settings.noteLifecycle, no deckImportSnapshots guard; deckName added to the record; the observed status is persisted so block banners become reachable; notes added in Anki are counted as "needs import" instead of a silent skip; empty ledger = no-op with a helpful hint; aggregate report; new "Sync current note" command for the active file)
- [ ] UC-29: deck vault -> Anki (read the Anki deck via cardsInfo, enforce the vault path's deck with changeDeck for the cards in the ledger deck only per SYNC-09)
- [ ] UC-30: tombstones + resurrect (vault delete event -> noteId -> deletedAt; resurrect iff Anki is newer than the deletion, otherwise respect the deletion; reverse index cleanup; both resurrection paths clear the tombstone: a Sync RESURRECT and a forced import, and Purge ledger forgets the record with it)
- [ ] UC-31: deletions (Anki deletion wins per SYNC-03: strip the block, purge the record, clean tombstones)
- [ ] UC-32: time tie (syncTieWinner + syncTieThresholdSec + ties reported)
- [ ] UC-33: correctness sweep per the audits: every vault scan respects ignoredDirectories per SYNC-10 (records in ignored folders are reported, never purged), pack check before ENROLL, purgedRecords in the Sync report, stale vault-index handling
- [ ] UC-33a: real-schema check for export (the model in a create's payload is decisive, but a push's modelName is inert - updateNoteFields cannot change a notetype, per ENT-07 - so a push must be checked against the model the note already has in Anki, while the field names it writes still come from the pack resolved from the block's model; that combination is silent data loss today and the exact case the built-in comparison cannot see: a block written before note-form had a model key reads as Basic (MODEL-09) while its Anki note is a Cloze with Text / Back Extra. Before planning, batch-read modelNames + modelFieldNames for every model in play, verify per note that the pack's field names are real fields of that note's model, and count a mismatch as "skipped on model mismatch" instead of letting AnkiConnect abort the whole multi. A create whose model is missing is still created from the built-in definition (UC-25a). This is the prerequisite for the mapping page in UC-36: without it the bulk report lies and one odd model kills the whole export)
- [ ] UC-34: report wording (non-blocking, needs UC-28 context): counts shown as notes (our tracked unit) with Anki card counts in parentheses? Proposed yes, awaiting confirmation.

Phase 3 - onboarding and UI (persona: ux/persona.md):
- [ ] UC-35: shared wizard shell per ENT-06 (page indicator, footer, cancel/back/next, progress) plus a shared notes table (pagination, field search, selection, per-row force toggle) and a shared report footer; the import wizard is migrated onto them with no behavior change
- [ ] UC-36: export wizard - the exact mirror of the import wizard, same page order and the same per-note force, only reversed (Deck: Anki deckNames as the list, each row showing how many vault notes map to it and how many are already in Anki -> Fields: the real field names of each target model, not our built-in definition; a model whose fields do not match the pack is mapped by the user here and saved as a pack, so the re-plan runs on a fixed mapping instead of a retry loop, since Fields comes before Notes; a model that does not exist at all can be created from that mapping - the field mapped to Front/Text is the card front, the one mapped to Back/Extra is the back, Skip fields are left out, and a one-sided mapping yields a one-field model with a front-only template - always behind an explicit confirmation, because it writes a new notetype into the user's collection; the bulk Export to Anki command never creates a type from a mapping, it has no way to ask: it may only create a missing built-in (UC-25a) and report the rest (UC-33a) -> Notes: validation (empty front, cloze markers, no pack, missing media) and per-row decisions, every row saying what will happen: creates a note / nothing to do / pushes your change / skipped, use Sync / edited in both, force / deleted in Anki, force to recreate -> Execute), reusing planExport as the candidate engine and passing the user's per-row decisions into exportActFor as isForced. Renames the command to "Export deck from Obsidian" (id export-deck-from-obsidian, mirroring import-deck-from-anki; no migration per MODEL-10) and mirrors the import side typographically: exportDeckCommandName, registerExportCommand, ExportWizard + ExportModal in src/gui/export-wizard/ next to the import wizard, ExportExecution beside ImportExecution, runExport moved into the modal; the report keeps the short "Export: ..." prefix and errors keep "Export failed: ..."; the force toggle is "Obsidian wins" with the same aria pattern as "Anki wins" and the same bulk button, and it must also cover vaultOnly.ankiDeleted (recreate), which the import side covers with RESURRECT
- [ ] UC-36a: import wizard note-selection UX (the notes preview is where every decision is made, so it must be obvious and fast; the shared table from UC-35 is the place, so the export wizard inherits it): a "Notes" column with the summary and the consequence badge, a selection column whose checkbox means "import this note" and is never silently greyed (a row the wizard will not write says so in the badge and explains on hover), and "Anki wins" as its own labelled column instead of two boxes crammed into one cell; the row label toggles the checkbox as well as the box itself; a header checkbox selects or clears the whole page and a second one every filtered row once search exists; the selected count moves into the table header next to the column name, and the bulk force button sits in the header bar of that column so the consequence line ("re-creates N notes you deleted in Obsidian") stays next to it; forced rows get their own visual treatment (colour from Obsidian theme vars plus the badge wording from UC-22d) instead of text only; keyboard: space toggles the focused row's selection, tab order runs row by row, and force is never reachable by a stray keypress; the "Cards to import" line becomes "Notes" together with UC-34; no behavior change to the decisions themselves, this is the affordance layer over importActFor (folds the two old backlog lines "label click toggles checkbox" and "vertically align checkbox with label")
- [ ] UC-37: single-note import/export flows (both wizards reachable from a note context with field search pre-filled; used by the future buttons)
- [ ] UC-38: mapping UI (simple-by-default: only undecided surplus rows; "all auto-mapped" state instead of an empty table; one-line summary of auto-mapped/skipped counts; settings mappingAdvancedView boolean, default false, revealing auto read-only rows + forced-Skip rows + pack metadata; ankiDefaultFields constant, extra->backExtra renames)
- [ ] UC-39: first-run "Start here" modal (permission -> import a deck or create a note; explains the three commands; the single Sync ribbon icon replaces the export icon; this is where ensureDefaultModels, the all-built-ins check, gets its caller - until then it is used only by tests)
- [ ] UC-40: New note command with a form (type picker: Basic, Basic (and reversed card), Basic (optional reversed card), Basic (type in the answer), Cloze; deck/folder preview; inserts a validated block), replacing the raw template commands
- [ ] UC-41: note-form edit validation + save feedback (front non-empty, cloze markers, tags normalisation, "no pack, this note will not export" warning, visible saved indicator, explicit conflict message instead of the silent drop in persister; blur stays the save trigger)
- [ ] UC-42: deck Anki -> vault (mirror a detected single-deck change as a file move to deckFolder, collision-safe renaming, ambiguous multi-deck notes reported and left alone per SYNC-09; a move into an ignored folder is reported, never performed)

Phase 4 - validation:
- [ ] UC-43: invariance tests (import, export and sync run twice = no-op; convergence after a forced overwrite)
- [ ] UC-44: end-to-end happy path on the test vault (new note -> export -> import -> sync, no duplicates in Anki or the vault)
- [ ] UC-45: regression "the import wizard never deletes" (no purge, no resurrection, no file removal in any path)
- [ ] UC-46: live AnkiConnect smoke (real model names and field names via curl; closes the UC-13 done-when, records the result in ux/persona.md notes or docs/synchronization.md; also records whether AnkiConnect rejects or silently drops a field name that is not in the target notetype, because the wording of the mismatch report depends on it and the pre-check in UC-33a must not contradict it)
- [ ] Round-trip fixtures from real cards (parked, back after sync)
- [ ] Integration tests: model simple paths (getSimplePaths) as regression pack
- [ ] Integration tests: reimport after note edit (no media duplicates)
- [ ] Integration tests: Cancel/error on Save, Anki down on every page
- [ ] GUI tests: Save without folder, label<->checkbox, selection reset, report/error

Backlog (small UI items, any time):
- [ ] ImportWizard: extract syncState useMemo into named buildNoteSyncState
- [ ] Export: not covered by a test - inserting a note-form block when a note with the same content already exists in Anki (the case the removed TODO comment in main.ts pointed at)
- [x] Wizard Notes: clear note selection on deck change (wrong counter) (done in UC-22: the deck handler clears the selection and the force decisions)
- [ ] Sync: file hash per note + diverged state (detect Obsidian-side edits; subsumed by synced.vaultNewer/hash work, kept as reminder)
- [ ] Ignored directories surfaced in the UI: a line plus tooltip listing the folders on both wizard Scope pages, a Settings hint that the rule is plugin-wide, and a report counter for notes skipped because of it
- [ ] Note entity duplication: basic-note, cloze-note and custom-mapped-note repeat `toPayload` and the media handling (47 of the 68 duplicated lines left in `src`), and export.ts vs note-push.ts repeat the update flow (17 lines) - do it while touching those files for UC-36 (the export creator) or ENT-01/ENT-04, whichever comes first
- [ ] Export media filenames: only the basename is used, so two same-named files in different folders collide in Anki
- [ ] README: still describes the removed generate command (vault-wide generation, legacy syntax)
- [ ] Settings: lastSyncRev is only a fallbackRev for the wizard deck counters; drop it with the ledger work
- [ ] Stricter tsconfig after the easy flags of UC-25b: exactOptionalPropertyTypes, noPropertyAccessFromIndexSignature (noUncheckedIndexedAccess is already on since UC-25c)
- [ ] Jest coverage thresholds (branches, functions, lines, statements), once the knip/jscpd cleanup has settled the real numbers
- [ ] Stryker mutation testing, limited to the lifecycle and sync services

Open decisions:
- [ ] UC-34 report wording (moved out of UC-07 so the wizard rename is not blocked).
